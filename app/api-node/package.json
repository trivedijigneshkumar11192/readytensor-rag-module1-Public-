{
  "name": "readytensor_rag_module1",
  "version": "1.0.0",
  "private": true,
  "description": "RAG Health Chatbot (Node API + Python embeddings, one-click start)",
  "type": "commonjs",
  "main": "app.js",
  "scripts": {
    "serve:api": "node app.js",
    "serve:emb": "node scripts/serve-emb.js",

    "prestart": "node scripts/prestart.js",
    "start": "concurrently -k -n EMB,API -c yellow,cyan \"npm:serve:emb\" \"npm:serve:api\"",
    "dev": "concurrently -k -n EMB,API -c yellow,cyan \"npm:serve:emb\" \"nodemon app.js\"",

    "setup:emb:win": "cd ..\\embeddings-py && (if not exist venv\\Scripts\\python.exe python -m venv venv) && venv\\Scripts\\pip install -r requirements.txt",
    "setup:emb:nix": "cd ../embeddings-py && ( [ -x venv/bin/python ] || python3 -m venv venv ) && . venv/bin/activate && pip install -r requirements.txt",

    "build:chroma:win": "cd ..\\embeddings-py && venv\\Scripts\\python build_chroma.py",
    "build:chroma:nix": "cd ../embeddings-py && venv/bin/python build_chroma.py"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "nodemon": "^3.1.10"
  }
}
